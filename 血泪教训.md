# KGIN中使用kg_final.txt，entity表示KG中全部节点，包括items；其adj mat shape=[n_node, n_node], n_node=n_entity + n_user.
# n_relation不包含ui交互关系

# MRAM中使用kg.txt，entity也已经包含item，使用kg.txt构建kg_graph，使用CF数据构建ui_mat, adj_mat
